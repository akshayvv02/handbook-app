version: '3.8'

services:
  nginx-app1:
    image: nginx:alpine
    container_name: nginx-app1
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./html:/usr/share/nginx/html:ro
